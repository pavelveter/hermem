name: provenance
steps:
  - name: store_with_provenance
    cli:
      args: ["memory", "store"]
      stdin: '{"id":"e1","category":"world","content":"Paris is the capital of France"}'
    expected:
      exit_code: 0

  - name: query_provenance
    cli:
      args: ["graph", "provenance", "--conversation", "conv-1"]
    expected:
      exit_code: 0
